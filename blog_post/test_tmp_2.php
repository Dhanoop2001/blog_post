<?php require "vendor/autoload.php"; $app = require_once "bootstrap/app.php"; $app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap(); config(["mail.mailers.smtp.verify_peer"=>false]); config(["mail.mailers.smtp.port"=>465]); config(["mail.mailers.smtp.encryption"=>"smtps"]); use Illuminate\Support\Facades\Mail; try { Mail::raw("Test body", function($msg) { $msg->to("dhanoop.ptf@gmail.com"); $msg->subject("Test email"); }); echo "SUCCESS\n"; } catch (\Exception $e) { echo "ERROR:" . $e->getMessage() . "\n"; }
